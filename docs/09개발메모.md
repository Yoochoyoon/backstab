## 개요

`01`~`08` 기획 문서를 바탕으로 실제 플레이 가능한 웹앱 MVP를 만들었다. 이 문서는 기획 문서에는 없는, 이번 개발 세션에서 정해지거나 알아둬야 할 것들만 정리한다(설치·실행법은 `README.md` 참고, 게임 규칙 자체는 `01`~`08` 참고).

## 프로젝트 위치가 바뀌었다

- 원래 기획 문서는 OneDrive(`C:\Users\cy060\OneDrive\집중표적게임`)에 있었지만, 코드(특히 `node_modules`)는 OneDrive 동기화 폴더와 궁합이 안 좋아서 **코드+문서+옵시디언 vault를 전부 `D:\집중표적게임`으로 통합 이동**했다. OneDrive 쪽 폴더는 이제 비어 있다.
- Claude Code를 다시 실행할 때는 **`D:\집중표적게임`에서 열어야** 경로가 맞는다. (이번 세션은 예전 OneDrive 경로를 기준 폴더로 잡은 채 시작됐지만, 파일 작업은 전부 절대경로로 `D:\집중표적게임`을 대상으로 했다.)

## 개발 로그 & 스킬

- **`docs/10개발로그.md`**: 세션마다 완료한 작업을 날짜별로 기록하는 누적 로그. `/개발로그` 스킬로 관리.
- **`.claude/skills/커밋푸시/SKILL.md`**: `git add`, `commit`, `push`를 한 번에 처리하는 `/커밋푸시` 스킬. 프로젝트의 자동 커밋 워크플로우 용.

## 기술 스택 & 실제 구현 범위

- **스택**: Node.js + TypeScript + Express + Socket.IO 백엔드, 빌드 도구 없는 순수 HTML/CSS/JS 프론트(React 사용 안 함), Render.com 배포 예정.
- **상태 저장**: 메모리에만 저장 — DB 없음. 서버가 재시작되면 진행 중이던 모든 방이 초기화된다(MVP 한정 허용).
- **MVP 구현** (2026-08-06): 8명 고정 입장, 역할 자동배정, 닉네임 중복 방지, 1라운드 탐색 라운드(스파이 서로 정체 확인, 전투 없음), 2라운드부터 기본 공격 + 역할별 특수 능력(보스 긴급처형·조직원 육탄방어/충성심서약·스파이 교란작전·배신자 흑막의미소) 처리, 낮(결과공개 → 토론 → 투표), 동점 시 재투표, 승리조건 판정, 진행자의 타이머 강제진행·연장, 첫판(1~2라운드) 힌트 툴팁, 진행자/TV 화면 통합.
- **Phase 2 완료** (2026-08-06):
  - ✅ 인원수 6~10명 가변: ROLE_COMPOSITIONS 룩업 테이블, MIN_PLAYERS/MAX_PLAYERS 상수, 로비 버튼 레이블 동적 변경.
  - ✅ 재접속 자동 복귀: sessionId 기반 플레이어 복원, localStorage 저장, state:full_sync 이벤트.
- **아직 구현 안 된 것** (기획 문서엔 있지만 코드엔 없음): 스파이 "암둠의 공모"(밤중 실시간 합공 신호 UI, 백엔드 미구현), 보스 "지명 수배"(공개 표적 지정, 백엔드 미구현), 배신자 "이중 스파이" 패시브(대응하는 "조사" 메커니즘 자체가 없어 보류), 사망자 관전 UI(사용자 요청으로 불필요 처리).

## 코드 짜면서 새로 정한 것들 (기획 문서엔 없던 세부사항)

- **진행자(host)는 8명 역할에 포함되지 않는 별도 컨트롤러**로 구현했다. `06컴포넌트테크구현.md`에는 "진행자도 역할을 겸해 참가할 수 있다"고 돼 있지만, MVP에서는 진행자=관전+컨트롤 전용이고 겸직은 2차 작업으로 미뤘다.
- **TV 화면은 자동 연결이 아니라 방 코드를 직접 입력**해서 연결한다(추후 QR코드 등으로 개선 가능).
- **자기 자신은 밤 지목/낮 투표 대상에서 제외**된다(자기공격·자기투표 불가) — 문서에 명시되지 않아 상식적으로 정한 부분.
- **결과 공개(낮 데미지 공개) 페이즈는 타이머가 없다** — 진행자가 화면에서 "토론 시작하기" 버튼을 눌러야 다음(토론)으로 넘어간다.
- **방 코드는 헷갈리는 문자(0/O, 1/I)를 제외한 4자리**로 자동 생성된다.

## 능력별 세부 구현 결정 (04핵심메커니즘.md 기준으로 코드로 옮기며 확정한 것)

- **육탄 방어 절반 경감은 내림(floor)** 처리. 예: 데미지 3 → 절반 경감 시 1.
- **육탄 방어 쿨타임 "1라운드"** = 사용한 라운드(N)의 바로 다음 라운드(N+1)엔 사용 불가, N+2부터 재사용 가능.
- **교란 작전은 그 자리(같은 라운드)에서 대상의 밤 행동 전체를 무효화**한다 — 모든 행동이 동시 제출되므로, 서버가 방어/공격 단계보다 먼저 침묵 대상을 판정해 걸러내는 방식으로 구현.
- **흑막의 미소의 HP 회복은 "이번 라운드 밤 전투로 발생한 사망자 수 × 2"** 만 반영한다(낮 투표 사망은 별개 페이즈라 카운트하지 않음).
- **배신자 "이중 스파이" 패시브는 보류**: 원래 "조사 카드/능력을 받으면 50% 확률로 위장"이라는 규칙인데, 이 MVP엔 대응하는 "조사" 계열 능력 자체가 없어 트리거할 지점이 없다. 나중에 조사형 능력이 추가되면 같이 구현.
- **스파이 "암둠의 공모"(밤중 실시간 합공 신호)도 보류**: 정보 노출 UI라 별도 설계가 필요해 이번 스코프에서 제외.

## 검증한 것

- `resolveRound.ts`(밤 데미지 합산·능력 처리·낮 투표·승리조건 판정) 유닛 테스트 16개 통과.
- 타입체크(`npx tsc --noEmit`) 통과.
- `socket.io-client`로 8명+진행자를 실제 로컬 서버(`npm run dev`)에 접속시켜 방생성→입장→1라운드 정찰(스파이 상호 확인 확인)→2라운드 밤에 조직원 육탄방어(흡수)로 보스 피해를 대신 받는 것까지 소켓 통신으로 end-to-end 검증(스크립트는 검증 후 삭제, 커밋 안 됨).
- 브라우저 여러 탭으로 8명 풀 플레이: 1라운드 정찰 → 2라운드(보스 긴급처형 2배 데미지, 조직원 충성심서약으로 공격 무효화) → 3라운드(힌트 소멸, 소진 능력 제거) 시나리오 직접 확인. Render.com 라이브 배포(https://jipjung-game.onrender.com)에서도 전체 흐름 정상 작동 검증.
- 진행자/TV 화면 통합: 하나의 페이지에서 위쪽은 공용 TV 영역(플레이어 상태, 결과 표시), 아래쪽은 진행자 컨트롤(강제진행, 시간연장)을 함께 표시.

## 현재 진행 중 (2026-08-06~)

**Phase 2 UI/UX 개선** — 느와르(고전 마피아 사건 기록실) 테마 적용
- **설계 완료** (`design.md` 17장): CSS 색상 팔레트(hex 16개), 웹폰트(Noto Sans/Serif KR, Special Elite, Cormorant Garamond), 화면별 상태(로비·게임·종료), 애니메이션(표적 펄스·타이머 경고), 반응형 규칙(모바일/태블릿/TV).
- **구현 계획**: Task 1~9, 진행자/TV 화면 우선, 플레이어 화면은 별도 계획.
- **버그 수정**: HP바 계산을 역할별 MAX_HP(보스 5, 나머지 4) 기준으로 수정.
- **스코프**: 백엔드 미구현 능력 2종(보스 지명수배, 스파이 어둠의 공모) UI 제외, 순수 CSS/HTML(이미지 에셋 없음)로 MVP 구현.

## 향후 예정 (Phase 3 이상)

1. **플레이어 화면 느와르 UI** — 개인 화면도 동일 테마 적용, 능력 카드 5종(백엔드 구현 분만).
2. **백엔드 미구현 능력 구현** — 보스 "지명 수배", 스파이 "어둠의 공모" 추가.
3. **선택적 에셋** — 역할/행동 아이콘(SVG), 조직 인장, 수배 도장.
4. **기타 개선** — 로그인/통계/게임 히스토리, 모바일 앱화, 다국어 지원.

## 다음에 할 일 (2026-08-21 기준)

- **QA 하네스를 저장소로 옮길지 결정 필요(사용자 판단)**: 헤드리스 크롬을 CDP로 직접 모는 QA/스크린샷 스크립트를 이번엔 스크래치패드에서만 돌렸다. `scripts/`에 넣어두면 스크린샷을 다시 뽑거나 회귀 검사를 돌릴 때 매번 새로 짜지 않아도 되지만, 개발 의존성이 아니라 임시 도구 성격이라 저장소에 둘지는 취향 문제다. `ws`는 socket.io를 통해 이미 들어와 있어 추가 설치는 필요 없다.
- **`/tv` 라우트 정리**: `public/tv.html`·`tv.js`가 남아 있는데 진행자 화면(`/host`)이 TV 역할까지 흡수한 뒤로 쓰이지 않는다. README에서는 이미 언급을 뺐다. 실제로 지울지, 남겨둘지 확인 필요.
- **미구현 능력 2종**: 보스 "지명 수배"(공개 표적 지정), 스파이 "암둠의 공모"의 백엔드는 아직 없다. 스파이 실시간 조율은 `spy:teammate_preview`로 사실상 대체돼 있으나 문서상 능력과 1:1로 맞진 않는다.
- **게임 도중 재접속 육안 확인**: 밤 행동/투표 제출 후 새로고침하는 흐름은 유닛 테스트와 소켓 레벨로만 검증했다. 실제 브라우저로 눈으로 본 적은 아직 없다.

## `/자동QA` 실행 후 다음에 할 일 (2026-08-16)

이번 회차에서 찾은 문제는 다 고쳤다(자세한 원인은 `10개발로그.md` 해당 날짜 항목 참고). 아래는 **이번엔 손대지 않고 남긴 것들** — 범위 밖이거나 판단이 필요해서 보류했다.

- **아이패드 세로(820×1180) 화면은 이번엔 못 봤다** — 가로(1180×820)만 검증했다. 반응형 구조상 문제 생길 가능성은 낮지만(가로가 더 좁은 쪽이라 통과하면 세로도 대개 통과), 세로 전용으로 걸리는 레이아웃 버그가 있을 수 있으니 다음 QA 때 포함시킬 것.
- **게임 도중 재접속(밤 행동/투표 제출 후 재접속) 화면은 유닛 테스트로만 검증됐다** — 이번 QA는 로비 단계의 재접속 경쟁 상태(방금 고친 버그)에 집중했다. 실제 브라우저로 "밤 지목 후 새로고침 → 재접속" 흐름을 눈으로 확인하는 건 아직 안 했다.
- **8인 정확히 채운 진행자 화면 스크린샷은 못 남겼다** — 테스트 중 닉네임 하나가 8자라 서버가 거부해서(7자 제한) 7명으로 진행됐다. 6인·10인(최소·최대)은 확인했고 7인도 이상 없었지만, 정확히 8인(기본값) 스크린샷은 다음 기회에 남겨도 좋다.
- **판단해서 정한 것**: `/자동QA` 스킬의 각 승리조건 검증은 8인 고정으로만 돌렸다(6·10인은 UI 확인 용도로만 사용, 승리조건 자체는 인원수를 안 바꿔도 됨 — `checkWinner()`가 인원수와 무관한 순수 함수라서 판정 로직이 갈릴 이유가 없다는 판단). 이견 있으면 다음에 인원수별로도 승리조건을 한 번씩 태워볼 것.
