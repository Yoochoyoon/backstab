## 개요

`09개발메모.md`가 한 시점의 스냅샷 메모라면, 이 문서는 세션마다 실제로 한 작업을 **날짜별·시간순(최신이 위)** 으로 누적 기록하는 개발 로그다. `/개발로그` 스킬이 이 파일을 관리한다.

**게임명:** Backstab (backstab-game, 느와르 테마 소셜 디덕션 게임)

---

## 2026-08-15

**온라인 플레이 지원 1단계 — 진행자 화면 없이도 게임이 돌아가게** — 이 게임은 원래 "같은 방에 모여 TV 한 대를 같이 보는" 오프라인 파티게임 전제로 설계돼 있어(`06컴포넌트테크구현.md`), 원격으로 하려면 빠진 게 셋 있었다: ① 대화 수단이 아예 없음 ② 결과·생존자 정보가 진행자 화면에만 상시 존재 ③ `day_reveal`에 타이머가 없어 진행자가 없으면 게임이 영구 정지. 셋 다 해결했다.
- **`day_reveal` 자동 진행**: `PHASE_DURATIONS_MS`에 30초를 넣고 `scheduleTimedPhase`로 넘겨 진행자 없이도 토론으로 이어지게 함. 진행자의 "토론 시작하기" 버튼은 조기 진행용으로 그대로 유지.
- **방장 겸 플레이어 모드**: 새 `player:create_room`으로 폰에서 바로 방을 만들고 그 사람이 방장(시작·진행 권한)을 겸한다. 진행자용으로 사람 1명을 더 구할 필요가 없어졌다. 기존 `/host` 오프라인 TV 모드는 그대로 공존.
- **플레이어 화면 상시 현황판**: 밤 결과가 2.4초 슬라이드로 스치고 사라지던 것을 결과공개~투표 내내 유지하도록 바꾸고, 페이즈와 무관하게 생존자·HP·역할을 보여주는 읽기 전용 로스터(`#rosterSection`)를 추가. 진행자 화면을 못 보는 원격 참가자도 자기 폰만으로 상황 파악이 된다.
- **텍스트 채팅**: 낮 페이즈에만 열리고 밤에는 서버가 발언을 거부한다(밤의 정보 비대칭이 이 게임의 핵심이라 스파이 밤 채팅도 넣지 않음 — 기존 `spy:teammate_preview` 신호로 충분). 사망자는 `03라운드진행.md` 규칙대로 읽기 전용. 재접속 시 대화 복원.
- **게임 종료 시 전원 역할 공개**: `publicPlayers()`가 종료 후에도 생존자 역할을 가려서 "누가 스파이였는지"를 영영 못 보던 문제를 `revealedPlayers()`로 해결. 사회적 추리 게임에서 제일 재미있는 순간이 빠져 있었다.

**낮 투표를 2단계로 — 지목 투표 + 찬반 심판** — 지목이 곧 처형이라 낮이 단순 다수결로 끝나던 것을, "누구를 심판대에 세울지"(지목 투표)와 "실제로 칠지"(찬반 심판) 두 단계로 나눴다. 지목만으로는 피해가 없고, 지목된 사람을 진행자 화면 한가운데와 각자 폰에 **수배 포스터처럼 크게 띄운 채** 찬반을 받아 가결돼야 데미지 1이 들어간다. 원래 논의하던 보스 "지명 수배"(단순 표시)를 대신하는 기능이면서, 지목된 사람의 변론이 한 번 더 들어가 토론이 살아난다.
- **밸런스 결정**: 가결 효과는 기존 낮 투표 데미지 1을 그대로 유지(03라운드진행.md의 "밤 공격이 더 무거운 수단" 원칙 보존). 찬성 > 반대여야 가결이고 동수·기권은 부결. 대상자 본인도 투표 가능.
- `resolveDayVote`를 집계만 하는 `tallyDayVote`와 심판을 처리하는 `resolveJudgement`로 분리하고 유닛 테스트 8개를 붙였다(총 35개).
- 심판 단계에도 채팅을 열어뒀다 — 변론할 수 없으면 이 단계를 넣은 의미가 없다.

**폰에서만 밤 스킬 목록이 안 보이던 버그 수정** — "데스크톱 창을 모바일 폭으로 줄이면 보이는데 진짜 폰에서는 안 보인다"는 제보로 잡은 버그. CSS 문제가 아니라 **재접속 시 비공개 정보가 복원되지 않는 문제**였다. `player:night_options`(밤 스킬 목록)와 `player:spy_reveal`(동료 스파이 명단)은 페이즈 시작 때 해당 플레이어에게 딱 한 번만 보내는데, `player:reconnect`는 `state:full_sync`만 보내고 이 둘을 다시 보내지 않았다. 그래서 폰이 탭을 버렸다 다시 열면(모바일 브라우저의 흔한 탭 폐기) 클라이언트의 `myNightOptions`가 기본값 `{specialActions: []}`으로 시작하고, `renderActionChoices()`가 `actions.length <= 1`이면 섹션을 통째로 숨기는 로직 때문에 스킬 영역이 사라졌다. 데스크톱에서는 탭이 안 버려지니 재현되지 않았다.
- `emitPrivateStateTo()`를 추가해 재접속 시 밤 스킬 목록과 스파이 동료 명단을 다시 보내도록 수정.
- 순수 소켓 시나리오로 검증: 스파이 소켓을 끊고 **완전히 새 소켓 + 저장된 세션**으로만 복귀시켜 두 정보가 모두 복원되는 것을 확인. 페이지 새로고침 경로도 별도로 확인.

**찬반 심판 연출 다듬기 (진행자 요청)** — 심판 포스터가 HP 카드 위에 그냥 얹혀서 뭘 가린 건지 애매해 보인다는 피드백을 받아, 심판 중에는 무대(위쪽)를 반투명 검정(`rgba(0,0,0,0.72)`)으로 눌러 포스터만 도드라지게 했다. 진행자 조작 바("시간 연장"/"다음 단계로")는 어둡게 덮지 않고 그 위로 올려 비활성처럼 보이지 않게 함. 이후 포스터를 더 키워달라는 요청에 이름 글자를 `min(20vh, 17cqw)`까지 키우되, 최대 길이(7자) 닉네임이 3줄로 접혀 화면을 다 먹는 걸 막기 위해 글자 수(`--name-len`)에 맞춰 폰트가 자동으로 줄어들도록 `calc()`를 걸고 한 줄 고정(`white-space: nowrap`)으로 바꿨다 — 짧은 이름은 최대 크기로, 긴 이름은 가로로 늘어나며 항상 한 줄에 담긴다.
- 구현 중 `.tv-upper`가 `z-index:4 + position:relative`로 별도 쌓임 맥락을 만들고 있어서 포스터(`z-index:6`)가 그 안에서 배경 위로 못 올라오던 문제를 발견 — 포스터를 `.host-layout`의 직계 자식으로 옮겨 해결.

**온라인 플레이 지원 2단계 — 안정성**
- **방/세션 자동 청소**: 방과 세션이 메모리 `Map`에만 있는데 만료 로직이 아예 없어서, 끝난 게임과 버려진 방이 프로세스가 죽을 때까지 계속 쌓이기만 했다(느린 메모리 누수). `Room.lastActivityAt`을 추가하고 10분마다 정리한다 — 끝난 방은 30분, 진행 중인데 아무도 안 돌아오는 방은 6시간 뒤. 판정 로직은 순수 함수(`isRoomExpired`/`cleanupExpiredRooms`)로 뽑아 유닛 테스트 6개를 붙였다.
- **서버 재시작을 조용한 실패에서 명확한 안내로**: 재시작되면 재접속이 "세션을 찾을 수 없습니다"로 실패하는데 사용자는 이유를 알 수 없었다. `server:hello`로 서버 부팅 시각을 내려보내고, 저장된 세션이 그보다 오래됐을 때만 "서버가 재시작되어 이전 게임이 종료되었습니다"라고 안내하고 세션을 정리한다(일시적 실패로는 지우지 않는다).
- **로비에서 끊기면 방이 통째로 사라지던 문제**: 로비 이탈은 즉시 제거였기 때문에, 방을 만든 사람이 폰 화면을 한 번 껐다 켜는 것만으로 (혼자였다면) 방이 삭제되고 이미 나눠준 방 코드가 무효가 됐다. 방장 모드에선 치명적이라 1분 유예를 두고, 로비 상태에서도 재접속을 허용하도록 고쳤다(대기실 화면·방 코드·시작 버튼까지 복원).
- **탭마다 세션이 섞이던 문제**: 세션을 `localStorage`에만 저장해서 같은 브라우저의 여러 탭이 서로의 세션을 덮어썼다. 나중에 들어온 탭이 앞 탭의 세션을 지우는 바람에, 새로고침하면 **남의 플레이어로 재접속**되는 일이 생겼다(실기기 여러 대로 하는 실제 플레이엔 영향 없지만, 봇 탭 QA는 계속 깨졌다). 탭 전용 `sessionStorage`를 우선 저장소로 쓰고 `localStorage`는 브라우저를 완전히 닫았다 연 경우의 보조로 두도록 바꿨다.

**온라인 작업 중 발견해 같이 고친 버그 3종**
- **재투표 시 제출 배지가 안 뜨던 문제**: `state:vote_result`가 넣어준 동점자 제한(`voteAllowedTargetIds`)을 곧바로 이어지는 `day_vote` 진입 처리가 매번 `null`로 덮어써서, 재투표인데 아무나 찍을 수 있게 보이고 서버는 그 표를 조용히 버렸다. 초기화 시점을 `day_discussion` 진입으로 옮겨 해결(재투표는 토론을 건너뛰므로).
- **재접속 시 같은 문제**: `state:full_sync`가 `voteAllowedTargetIds`를 보내주는데도 클라이언트가 읽지 않고 있었다.
- **채팅 전송 버튼이 입력창을 25px로 찌그러뜨리던 레이아웃 버그**: 전역 `button { width: 100% }` 규칙이 `flex: none`을 이겨서 버튼이 줄 전체를 차지했다. `width: auto`로 되돌려 해결.

**실제 폰으로 처음부터 끝까지 풀 플레이 테스트** — 봇 5명 + 사용자 본인 폰(보스 역할)으로 로비 입장부터 게임 종료(스파이 승리)까지 실제 게임을 진행하며 페이즈 슬라이드 4종(밤 시작/밤 결과/토론 시작/투표 시작), 사망 연출, 보스 위급 상태(빨간 테두리) 강조를 전부 실제 플레이 중 스크린샷으로 확인. 이 과정에서 폰 화면 스크린샷을 통해 드러난 실사용 버그 여러 개를 발견해 수정함.

**재접속 시 스킬/HP바가 안 뜨던 근본 원인 수정** — `player:reconnect` 핸들러가 세션은 검증하면서도 `player.id`(과 세션의 `playerId`)를 새 소켓 id로 갱신하지 않아서, 재접속 이후엔 `io.to(player.id).emit(...)`으로 보내는 밤 행동 옵션이 끊어진 예전 id로 날아가 아예 도착하지 못하고, `room.players.find(p => p.id === socket.id)`로 찾는 밤 행동/투표 제출 핸들러도 조용히 실패하고 있었다. 재접속 시 `player.id`/`session.playerId`를 새 `socket.id`로 갱신하도록 수정 — 폰을 새로고침해도 스킬 선택과 투표/공격 제출이 계속 정상 동작한다.

**HP바 계산 버그 수정** — 플레이어 화면의 HP 바가 역할별 최대 HP(보스 5, 그 외 4) 대신 무조건 5로 나눠 계산되고 있어서 보스가 아닌 역할은 항상 실제보다 낮은 비율로 표시됐고, 재접속 직후에는 실제 HP와 무관하게 무조건 100%로 그려지고 있었다. `getMaxHpForRole()`을 써서 정확한 비율로 계산하도록 수정.

**스킬 설명 보강 및 카드 레이아웃 변경** — 기존에 "데미지 1", "쿨타임 1라운드"처럼 대충 적혀있던 6개 스킬(기본 공격/긴급 처형/육탄 방어/충성심 서약/교란 작전/흑막의 미소) 설명을 효과·제한 횟수까지 명확히 담아 다시 썼다. 설명이 길어져 기존 2열 카드에서는 짓눌리므로 `.na-skill-grid`를 1열로 변경.

**모바일 가독성/레이아웃 개선 3건**
- 페이즈 전환 슬라이드: 모바일에서 제목/설명 글씨 크기를 키우고 설명 텍스트에 검은 외곽선(text-shadow) 추가.
- 호스트 화면 결과 로그(`#resultLog`): `<br>`로 세로 나열되던 걸 `<span>` 기반 flex-wrap 가로 한 줄로 바꾸고 글씨도 키움.
- 호스트 화면(`.host-layout`)이 `height:100vh`를 쓰고 있어 모바일 브라우저 주소창 유무만큼 실제 보이는 영역보다 커지고, `overflow:hidden`과 만나 하단(`.control-lower`의 결과 로그/시간 연장/다음 단계로 버튼)이 화면 밖으로 잘려 안 보일 수 있던 문제를 `height:100dvh`(동적 뷰포트 높이)로 교체해 해결.

---

## 2026-08-14

**진행자 게임 진행 화면(TV) 전면 재구성** — Figma "TV - 3" 기준으로 처음부터 다시 만듦.
- 사건번호:라운드:타이머 = 1:3:2 비율 한 줄, 참가자 HP 모니터가 남는 공간을 채우도록, 보스는 오른쪽 큰 카드로 배치.
- 참가자 HP 모니터에서 보스 제외 — 오른쪽 보스 카드에 이미 이름/HP가 있어서 중복이었음(`02역할.md` 기준 보스가 유일한 공개 역할이라는 근거로 판단).
- HP pip 크기/텍스트 확대, 밤·토론·투표 이모지를 플랫 SVG 도형(초승달/말풍선/체크박스)으로 교체하고 검정 배경+흰 아이콘, 현재 라운드만 노란 테두리로 강조.
- 사망한 참가자 카드에 어두운 오버레이 + 30도 기울어진 큼직한 "사망" 도장 텍스트 연출.
- 밤 페이즈에서는 카드/박스의 흑백을 반전(검정↔흰색)해서 분위기를 확실히 바꾸되 노랑·빨강 강조색은 유지, 350ms 트랜지션.
- 반복 발견된 버그 패턴: `element.style.display = "block"`이 CSS `display:flex`(gap 포함)를 인라인으로 덮어써서 조용히 레이아웃을 깨는 문제가 host.js/player.js 양쪽에서 여러 번 나왔음(bossBanner, gameSection, postJoinScreen, actionSection 등) — 전부 `"flex"`로 수정.
- CSS Grid/Flex 아이템의 기본 `min-width:auto`가 내용이 트랙보다 크면 안 줄어들고 그대로 잘려나가는 문제를 발견해(모바일도 데스크탑도 아닌 640~800px대 좁은 창에서 재현) 관련 요소 전부 `min-width:0` 추가.
- 디자인 통일: 기존 화면들과 같은 테두리 두께(`0.5vh` 표준, 참가자 카드만 `0.7vh`로 강조), 사건번호/라운드는 검정 테두리+흰 면, 타이머는 흰 테두리+검정 면으로 색 반전, 캐릭터 사진 자리는 그라데이션 대신 단색 검정, skewX 스티커 스타일은 반복된 잘림 문제로 전부 되돌려서 원래의 사각형 형태로.
- 배경은 새 색을 만들지 않고 랜딩 화면부터 쓰던 `.hero-card`(회전 카드+종이질감)를 그대로 재사용하도록 구조 변경.

**플레이어 밤 행동/투표 화면 신규 구현** — Figma "Android Compact - 3" 목업 기준.
- 보스공개/내역할/타이머 3박스, 스킬 선택(기본공격+역할별 특수능력 카드, 아이콘 포함), 대상 선택 그리드, 선택 요약, 지목 확정 버튼까지 전부 새로 구현. `#gameSection`이 밤/투표 공용이라 투표 페이즈에도 자동으로 대응.
- 대상 카드는 진행상황 화면의 `hp-monitor-card` 컴포넌트를 그대로 재사용(재활용 원칙) — 사망 도장 효과용 `overflow:hidden`이 위로 튀어나오는 "보스" 배지를 잘라먹는 문제만 이 화면 한정으로 `overflow:visible`로 풀어서 해결.
- 지목 완료 시 선택한 카드 테두리가 노랑→초록으로, "지목 확정" 버튼이 "지목완료"+어두운 톤으로 바뀌고 잠긴다.
- 서버가 `publicPlayers()`에서 `role` 필드를 아예 안 보내고 있어서 `p.role === "boss"` 체크가 실제 서버 데이터로는 한 번도 참이 된 적 없던 버그(수동 테스트 데이터로만 통과됐던 것)를 발견 — 보스는 공개 역할이므로 서버에서 `role:"boss"`만 흘려보내도록 수정. 참가자 HP 모니터의 보스 제외, 대상 카드의 "보스" 배지, HP 최대치 계산이 전부 이 수정 하나로 실제로 동작하게 됨.
- 재접속(`state:full_sync`) 시 서버가 방 전체 플레이어의 역할을 필터링 없이 그대로 보내고 있던 프라이버시 버그도 같이 발견해 `publicPlayers()` + 본인 역할만 별도 필드(`myRole`/`myHp`)로 분리해서 고침. 같은 경로에 보스 배너를 아예 안 띄우는 버그도 있었음(처음부터 구현이 빠져 있었음) — 같이 추가.
- 새 방에 "입장하기"를 누르면 이전 세션(localStorage의 sessionId/roomCode)을 먼저 지우도록 수정 — 안 그러면 같은 브라우저로 새로고침할 때마다 이전(어쩌면 중단된) 방으로 자동 재접속되는 문제가 있었음. 같은 방 안에서 연결이 끊겼을 때의 재접속은 그대로 유지.
- 모바일 전용 버그 3개: 타이머가 `--:--`(대기 상태)일 때 줄바꿈되며 깨지던 것(`white-space:nowrap` 누락), "대상을 선택하세요" 섹션이 밤/투표가 아닌 페이즈에서도 제목만 계속 떠 있던 것(내용만 지우고 섹션 자체는 안 숨겼던 게 원인), 게임 종료 시 상단 타이머/라벨이 직전 페이즈 문구를 그대로 들고 있던 것(`applyPhase()`가 game_over에서 이르게 return).

**페이즈 전환 슬라이드 신규 구현** — 밤 시작/토론 시작/투표 시작/밤 결과(사망자)/투표 결과(최종 지목자)마다 호스트·참가자 화면 양쪽에 잠깐 떴다 사라지는 전체화면 오버레이. `shared.js`에 `createSlideQueue()`를 만들어 host.js/player.js가 같은 방식으로 재사용하고, 큐 방식이라 연달아 오는 이벤트(예: 밤 결과 직후 바로 다음 라운드 시작)도 겹치지 않는다. 참가자가 호스트 화면 없이 자기 화면만으로 상황을 파악할 수 있게 하는 게 목적.

**신규 프로젝트 스킬 2개**
- `화면확인`: UI에 영향 주는 파일을 수정하면 사용자가 요청하지 않아도 자동으로 브라우저 스크린샷을 찍어 보여준다. 사용자의 실시간 테스트 세션(여러 플레이어 탭)은 절대 새로고침/재사용하지 않는다는 안전 수칙 포함.
- `봇채우기`: 사용자가 직접 플레이어로 테스트하고 싶을 때 방을 만들고(또는 기존 방에) 나머지 인원을 봇(자동 접속 플레이어 탭)으로 채운다.

**`docs/design.md` 20장 추가**: 테두리 두께 표, 진행 화면 박스 색 반전 규칙, 캐릭터 사진 placeholder 규칙, `.hero-card` 재사용법, `--btn-accent` 공용 버튼 패턴을 실제 구현 기준으로 문서화.

---

## 완료됨 (2026-08-06~2026-08-13)

**Phase 2 UI/UX 개선 — 느와르 테마 적용 완료**

- **느와르 UI 구현 최종 완료** (2026-08-13, Task 1~9):
  - Task 1: CSS 변수 + 웹폰트 (627e561)
  - Task 2: HTML 구조 + 시맨틱 클래스 (2caa92a)
  - Task 3: HP 바 버그 수정 (5e3c3bf)
  - Task 4: 기본 스타일링 202줄 (b442698)
  - Task 5: 페이즈별 배경 전환 (a7fc4ba)
  - Task 6: 애니메이션 (287bda1)
  - Task 7: 반응형 미디어 쿼리 (ab05353)
  - Task 8: 상태 클래스 통합 (71bcbba)
  - Task 9: 검증 + 문서화 (6c6792b)
  - **검증 결과**: 브라우저 테스트 완료, Lighthouse 60+, 반응형(375/768/1920px), 접근성 WCAG AA, 8명 게임 시뮬레이션 통과

- **다음 단계 준비 (2026-08-13)**:
  - Figma 디자인 대기 중 (진행자/플레이어/TV 모든 화면, 느와르 새 디자인, 애니메이션+캐릭터 이미지 포함 고려)
  - UI/UX 제공 가이드 문서 작성 (`UIUX-제공-가이드.md`): 화면별 마크다운 템플릿, 이미지 요구사항, 파일 구조, 불명확 항목 체크리스트 정의

- **진행자/TV 화면 느와르 UI 구현** (Task 1~8):
  - CSS 색상 팔레트(16개 변수): 종이색(`--color-paper: #e7d8b8`), 금색(`--color-gold: #c7a55a`), 크림슨(`--color-crimson-bright: #c33a3f`), 텍스트(`--color-text: #f0e7d4`), 배경(`--color-bg: #12131a`, `--color-bg-deep: #0b0c10`)
  - 웹폰트 4종: Noto Sans KR(본문), Noto Serif KR(제목), Special Elite(타이프라이터/레이블), Cormorant Garamond(라틴)
  - 시맨틱 HTML 클래스 추가: `.case-header`(CONFIDENTIAL CASE FILE), `.phase-status`(라운드/페이즈/타이머), `.boss-case-file`(큰 보스 카드+금색 테두리), `.player-file-grid`(그리드 레이아웃), `.player-file-card`(각 플레이어 카드)
  - HP바 버그 수정: 역할별 MAX_HP 반영(`보스 MAX=5, 나머지 MAX=4`) → 정확한 퍼센티지 계산 (5/5=100%, 4/5=80%, 4/4=100%, 3/4=75% 등)
  - 페이즈별 배경 전환: 밤(어두움 `color-bg-deep`) ↔ 낮(밝음 `color-bg`), 500ms `ease-out` 트랜지션
  - CSS 애니메이션: 표적 펄스(2.2s 무한, 대상 플레이어 강조), 타이머 경고 깜빡임(600ms, ≤30초 주황색/≤10초 크림슨색)
  - 반응형 미디어 쿼리: 모바일 360px(1열 그리드), 태블릿 768px(3열), TV 1200px+(4열 이상)
  - 상태 클래스 통합: `.is-dead`(투명도 50%), `.is-boss`(금색 테두리), `.is-critical`(크림슨 배경), `.is-warning`(주황색), `.is-targeted`(펄스 애니메이션)
  - 검증: Lighthouse 성능 60+, 텍스트 대비 4.5:1 이상, 터치 대상 44×44px 이상, 8명 게임 시뮬레이션 완료

- **UI/UX 개선 디자인 보완 사양 작성**: `design.md`의 17장 "구현 보완 사양" 절 추가. 느와르 스타일 실제 구현을 위해 CSS 색상 팔레트(hex 값 16개 정의), 웹폰트 사양(Noto Sans KR/Noto Serif KR + Special Elite + Cormorant Garamond), 기존 HTML 엘리먼트와 디자인 개념 매핑, 전체 화면 상태(로비/재접속/오류/종료), 7개 능력 카드 중 백엔드 미구현 2종 MVP 범위 제외, HP바 역할별 MAX_HP 버그 수정, 애니메이션·인터랙션 타이밍표, 모바일/TV 반응형 규칙, 에셋 전략 명시. 이번 구현 기준문서로 활용.

## 2026-08-06 (계속)

- **재접속 자동 복귀 구현**: 플레이어가 연결을 끊었다가 재입장할 때 sessionId를 통해 기존 플레이어 상태를 자동 복원. 백엔드: 세션 저장소(Map) + createSession/getSession/deleteSession/deleteSessionsByRoom 함수, reconnect 핸들러(세션 검증 → 상태 복원), disconnect 핸들러(로비: 제거, 게임: 보존). 클라이언트: localStorage에 sessionId/roomCode 저장, 페이지 로드 시 자동 재연결 시도, state:full_sync 이벤트로 게임 상태 복원. 검증: `npx tsc --noEmit` 통과, `npm test` 23/23 통과.
  - 설계 특성: 세션 ID 방식(UUID), 게임 진행 중에만 재연결 허용, 게임 종료 시까지 세션 유지, 현재 상태만 동기화(히스토리 재생 안 함).

- **인원수 가변 구현 (6~10명)**: `docs/05인원세션타겟층.md`에 정의된 역할 배정표를 코드로 구현. `ROLE_COMPOSITION` 고정 배열(8명)을 `ROLE_COMPOSITIONS` 룩업 테이블(6~10명 각각)로 교체. 로비 UI 버튼 레이블도 "시작 (N/8명)" → "시작 (N/6~10명)"으로 변경해 사용자 명확성 증대. 서버(range 검증) + 클라이언트(button state) 양쪽 범위 체크 추가. 기존 `resolveRound.test.ts` 16개 + 신규 `roleAssignment.test.ts` 7개(6~10명 유효 케이스 5개 + 범위 외 거부 2개) = 총 23개 테스트 통과.
  - 검증: `npx tsc --noEmit` 통과, `npm test` 23/23 통과, 브라우저에서 로비 플레이어 입장 시 버튼 레이블/disabled 상태 올바르게 변경되는 것 확인.

## 2026-08-06

- **진행자/TV 화면 통합**: `/host`와 `/tv` 라우트를 하나의 페이지로 합침. 위쪽은 공용 TV 영역(플레이어 상태, 보스 공개, 결과 표시), 아래쪽은 진행자 컨트롤(강제진행, 시간연장). 이제 진행자는 한 화면에서 모든 걸 볼 수 있고, 필요하면 그 화면을 빔/모니터에 HDMI로 연결하거나 화면공유만 하면 됨. `/tv` 경로는 하위호환성용으로 유지.
- **Render.com 배포 완료 및 라이브 확인**: Build/Start 명령 설정(npm install / npm start), 배포 성공 후 라이브 URL https://jipjung-game.onrender.com에서 진행자/플레이어/TV 화면 모두 정상 작동 확인. 플레이어 입장, 게임 진행 전체 흐름 테스트(8명 플레이 시뮬레이션)로 특수 능력 + 통합 UI 라이브 환경에서도 정상 작동 확인.

## 2026-08-05

- **HP 표시 버그 수정**: `server/src/socketHandlers.ts`의 `emitState()`에서 `state:players` 이벤트를 `state:phase_changed`보다 먼저 보내도록 순서 변경. 라운드 전환 시 플레이어 화면이 최신 HP를 못 읽고 잠깐 "HP 0"으로 표시되던 레이스 컨디션 해결.
  - 검증: `npx tsc --noEmit` 통과, 유닛 테스트 9/9 통과, 브라우저에서 8명 실제 플레이로 재현 안 됨을 확인.
- **git 첫 커밋 & GitHub 푸시**: 로컬 브랜치를 `master` → `main`으로 정리하고 `node_modules` 제외한 코드+문서+옵시디언 vault 전체(34개 파일) 커밋. `origin`을 `https://github.com/Yoochoyoon/-`로 연결해 푸시 완료.
- **Render.com 배포는 보류**: 사용자가 직접 진행하기 어려운 상황이라 다음 작업으로 순서 변경.
- **2차 작업 착수 — 특수 능력 7종 구현**: `02역할.md`·`03라운드진행.md`·`04핵심메커니즘.md` 기준으로 보스(긴급 처형)·경호원(육탄방어/충성심서약)·스파이(교란작전)·배신자(흑막의미소) 능력과 1라운드 정찰 라운드(스파이 서로 정체 확인, 공격 불가)를 구현 중. (진행 상황은 다음 로그 항목에서 이어짐)
- **개발로그 문서 & `/개발로그` 스킬 신설**: 이 파일(`10개발로그.md`)과 `.claude/skills/개발로그/SKILL.md`를 새로 만들었다. 윈도우는 폴더명에 `:`을 못 써서 사용자가 요청한 `/개발:로그` 대신 `/개발로그`로 이름 지음.
- **특수 능력 7종 구현 완료**: `types.ts`에 `NightAction`/`PlayerAbilityState` 도입, `resolveRound.ts`의 밤 해석 로직을 "교란(침묵) → 방어/보호 → 공격 합산 → 회복" 4단계 파이프라인으로 재작성, `socketHandlers.ts`에 1라운드 정찰(스파이 상호 정체 공개, 전투 없음)과 2라운드부터의 `player:night_options`/`player:submit_night_action` 배선 추가, `player.js`/`player.html`에 행동 선택(기본공격 vs 특수능력) UI 추가. 배신자 "이중 스파이" 패시브와 스파이 "암둠의 공모"(실시간 합공 신호 UI)는 대응하는 메커니즘이 없거나 별도 UI 설계가 필요해 이번 스코프에서 보류.
  - 검증: 유닛 테스트 16개 통과(능력별 개별 케이스 포함), `npx tsc --noEmit` 통과, `socket.io-client`로 실제 서버에 8명+진행자를 접속시켜 1라운드 정찰→2라운드 육탄방어(흡수) 시나리오를 소켓 통신으로 end-to-end 확인. 브라우저로 직접 눈으로 보는 테스트는 아직 안 함.
- **첫판 힌트 툴팁 구현**: `07룰복잡도온보딩.md` 기준으로 1~2라운드에만 페이즈별 짧은 안내문을 보여주고 3라운드부터 자동으로 사라지게 함(서버/데이터 변경 없이 `player.js`에서 이미 받고 있던 라운드 번호만으로 클라이언트에서 판단). 남은 2차 기능 중 서버 변경이 전혀 필요 없는 가장 단순한 항목이라 이걸 먼저 골랐다. 참고로 "닉네임 중복 방지"는 확인해보니 이미 기존 코드에 구현되어 있었음(메모가 안 맞았던 것) — `09개발메모.md` 목록에서 정정.
- **브라우저 8명 실전 테스트로 특수 능력 + 힌트 검증**: `npm run dev` 후 크롬 탭 10개(진행자/TV/플레이어 8)로 1라운드 정찰 → 2라운드(보스 긴급처형 실제 클릭으로 사용해 스파이에게 2배 데미지 확인, 경호원 충성심서약으로 공격 무효화 확인) → 3라운드(힌트 소멸, 소진된 능력이 목록에서 빠짐)까지 눈으로 직접 확인. 이 과정에서 `player.js`의 `renderActionChoices()`가 특수 능력이 하나도 없을 때 컨테이너를 숨기기만 하고 내부 `<li>`를 지우지 않던 사소한 버그(사용자에게는 안 보이지만 DOM에 잔재가 남는 문제)를 발견해 수정.
